version: 0.2

env:
  variables:
    S3_BUCKET: "codebuild-ap-south-1-644480493008-input-bucket"

phases:
  install:
    runtime-versions:
      java: corretto11 
  pre_build:
    commands:
      - echo Nothing to do in the pre_build phase...
  build:
    commands:
      - echo Build started on `date`
      - mvn install
  post_build:
    commands:
      - echo "Hello world this code buils project"
      - echo 'S3 bucket name is $S3_BUCKET" 
      
